<Window x:Class="InterviewProblem.View.UserTotalsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InterviewProblem.ViewModel"
        mc:Ignorable="d"
        Title="User Totals" Height="200" Width="300">
    <Window.DataContext>
        <vm:UserTotalsViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="10" Text="Number Passed:" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="10" Text="{Binding NumberPassed}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="10" Text="Number Failed:" />
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="10" Text="{Binding NumberFailed}" />
    </Grid>
</Window>
